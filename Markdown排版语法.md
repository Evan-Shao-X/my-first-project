# Obsidian 常用 Markdown 语法速查表

## 一、 标题 (Headings)
在文字前面加上 `#` 和空格，可以创建不同级别的标题。最多支持六级。

# 这是一级标题 (最大)
## 这是二级标题
### 这是三级标题
#### 这是四级标题

---

## 二、 文本排版 (Text Formatting)
*   **加粗**：在文字两边各加两个星号。例如 `**重要文字**` ➡ 效果为：**重要文字**
*   *斜体*：在文字两边各加一个星号。例如 `*倾斜文字*` ➡ 效果为：*倾斜文字*
*   ~~删除线~~：在文字两边各加两个波浪号。例如 `~~作废内容~~` ➡ 效果为：~~作废内容~~
*   ==高亮==：在文字两边各加两个等号。例如 `==核心重点==` ➡ 效果为：==核心重点== 

---

## 三、 列表 (Lists)

### 1. 无序列表
使用短横线 `-` 或者星号 `*`，加上一个空格：
- 列表第一项
- 列表第二项

### 2. 有序列表
使用数字、英文句号 `.`，加上一个空格：
1. 操作步骤一
2. 操作步骤二

### 3. 待办事项 (Task Lists)
使用短横线、空格、方括号，组合起来可以变成带有打钩框的任务清单。
- [ ] 这是一个还没做的任务 (注意括号中间有一个空格)
- [x] 这是一个已经完成的任务 (括号里填入小写的 x)

---

## 四、 引用段落 (Blockquotes)
在文字前面加上大于号 `>` 和一个空格，非常适合用来做摘抄或重点提示。
> 这里是一段引用的文字，或者是一句重要的总结。

---

## 五、 代码块 (Code)
*   **行内代码**：使用反引号（键盘左上角 Esc 键下方的那个键）把文字包起来。例如 `` `Ctrl+C` `` ➡ 效果为：`Ctrl+C`
*   **多行代码块**：连续输入三个反引号，换行后输入内容，最后再用三个反引号收尾。适合记录大段的代码或参数。

---

## 六、 分隔线 (Horizontal Rule)
在新的一行，连续输入三个短横线 `---`，然后按回车键，就会生成一条华丽的水平分割线，用来划分文章段落非常清晰。

---

## 七、 链接与外部插入 (Links)
*   **内部双链**：这是 Obsidian 的灵魂 `[[笔记名称]]`
*   **外部网页链接**：使用 `[显示的文字](完整的网址)`。例如 `[百度](https://www.baidu.com)`
*   **插入外部图片**：在网页链接的最前面加一个英文感叹号 `![图片名称](图片地址)`。（注：在 Obsidian 中处理本地图片时，**直接把图片拖拽进笔记界面最简单**，软件会自动为你生成对应的代码）。
